import Immuntable from "immutable";
import AppState from "../app_state.js"

function AdminReducer(state = AppState, action = null) {
    return state;
}


function RootReducer(state = AppState, action = null) {
    return state;
}

export {AdminReducer, RootReducer}